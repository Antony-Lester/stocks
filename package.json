{
  "devDependencies": {
    "@types/msgpack5": "^3.4.3",
    "@types/node": "^14.18.58",
    "@types/pg": "^8.10.2",
    "@types/pg-format": "^1.0.2",
    "@types/readable-stream": "^4.0.2",
    "@types/ws": "^8.5.5",
    "gts": "^3.1.1",
    "jest": "^29.6.4",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "~4.7.0"
  },
  "scripts": {
    "seed-test": "psql -h localhost -p 5432 -U postgres  -a -f /mnt/md0/code/stocks-mk2/src/db/setup-test.sql;",
    "seed-prod": "psql -h localhost -p 5432 -U postgres -a -f /mnt/md0/code/stocks-mk2/src/db/setup-prod.sql;",
    "prod-inital": "export NODE_ENV=prod; node build/src/run/runSetupAndInitalDownload.js",
    "prod-database": "export NODE_ENV=prod; psql -U antony -d stocks",

    "build": "gts clean; npx tsc",
  
    "lint": "clear; gts fix; npx prettier --fix; gts lint --fix",

    "pretest": "export NODE_ENV=test; npm run lint",
    "test": "jest --runInBand",
    "test-watch": "jest --runInBand --no-cache --forceExit",
    "test-clean": " export NODE_ENV=test; jest --clearCache; clear",
    "test-coverage": "export NODE_ENV=test; jest --coverage",
    "test-debug": "export NODE_ENV=test; env-cmd -f .env.test; node --inspect-brk node_modules/.bin/jest --runInBand --no-cache --forceExit",
    
    "test-database": "export NODE_ENV=test; psql -U antony -d stocks_test"
  },
  "dependencies": {
    "@alpacahq/alpaca-trade-api": "^3.0.1",
    "alpaca": "^1.5.27",
    "dotenv": "^16.3.1",
    "env-cmd": "^10.1.0",
    "moment": "^2.29.4",
    "moment-range": "^4.0.2",
    "node-pg-format": "^1.3.5",
    "pg": "^8.11.3",
    "pg-format": "^1.0.4"
  }
}
